{
  "name": "wolko-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "push": "git add -A && git diff --cached --quiet || git commit -m \"update\" ; git pull --no-rebase origin main && git push origin main"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
